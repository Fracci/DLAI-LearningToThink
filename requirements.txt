# DLAI-LearningToThink — runtime dependencies (pinned)
#
# Versions reflect the development environment. Install with:
#   pip install -r requirements.txt        (or: uv pip install -r requirements.txt)
#
# A CUDA-enabled PyTorch build is assumed for full reproduction of the 5-seed sweeps.
# If installing on CPU / a different CUDA version, adjust the torch line to the
# appropriate wheel from https://pytorch.org (the code uses the torch.amp unified
# device API, so torch >= 2.4 is required).

# --- core: model, training, AMP ---
torch==2.5.1

# --- data handling & numerics (torch dependency, pinned explicitly) ---
numpy==2.1.3

# --- results aggregation / reading .xlsx workbooks ---
openpyxl==3.1.5

# --- plotting ---
matplotlib==3.9.2

# NOTE: everything else the scripts use (csv, random, statistics, glob, os, sys,
# time, math, collections, dataclasses, typing) is in the Python standard library
# and does not need to be listed here.